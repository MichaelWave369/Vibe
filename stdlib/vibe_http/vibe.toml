[package]
name = "vibe_http"
version = "0.1.0"
description = "Vibe standard library HTTP intent contracts"

[bridge]
measurement_safe_ratio = 0.85
epsilon_floor = 0.02

[emit]
default_target = "python"

[dependencies]

[metadata]
domain = "general"
tags = ["stdlib", "http", "request-response", "phase-8.4"]
